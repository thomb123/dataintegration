apply plugin: "war"

bootWar {
    mainClassName = "com.totient.dataintegration.DataintegrationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
